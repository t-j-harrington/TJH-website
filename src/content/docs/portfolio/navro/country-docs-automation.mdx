---
title: Country docs automation
description: I have worked as a Technical Writer at Navro since November 2024.
banner:
  content: |
    This site is under construction.
    
    Check back over the course of Q3 2026 as I update it.
---

import { Card } from '@astrojs/starlight/components';

As a company, Navro offers _payout curation_. Put simply, if a client wants to pay a beneficiary anywhere in the world, Navro has a relationship with a variety of banking partners and integrations and routes the payout through the best option.

However, clients often want to route payouts through a specific _payment rail_. Some countries support multiple payment rails, such as wire, ACH, and instant payments. However, certain payment purposes require a specific payment rail.

For example, payments with taxes withheld to Romanian beneficiaries require you make the payment over the local payments infrastructure. However, for a cheaper and faster payment, you might use the SEPA Credit payment rail. =

As such, the Navro documentation needed to contain information for over 800 different payment rail and country/territory combinations. This way, the reader can always learn about the options they have for paying each country.

Given the frequency of updates, maintaining these manually as a single person wasn't feasible. So I set about automating the process.

<Card title="Role-based access control" icon="padlock">
The range of countries, payment methods, and features a given Navro client had access to depended on their contract with Navro.

This meant we needed to secure the documentation behind a login screen and only present relevant, personalised, documentation to each reader.

<a href="/portfolio/navro/role-based-access-control.md" target="_blank" >Learn more about how I implemented role-based access control at Navro</a>.
</Card>

## Country validation infrastructure

When using Navro, you can choose to make your payouts through a user-friendly user interface or an API. However, they're both driven by the same validation rules, expressed as a JSON file.

### The country validation JSON file

The validation rules JSON file tells you which fields are required and optional, allowed values, as well as regular expression explaining the allowed values for free text fields. Sometimes the response also contains a plain English explanation of the required format too.

The JSON file has a format like this (I've altered parameter names and formats to be different to Navro's product):

```json
{
  "data": [
    {
      "id": "9f2c7d14-6b3e-42a8-b0d5-1c84ef7a53b9",
      "paymentMethodName": "LOCAL_TRANSFER",
      "supportedCurrencies": [
        "EUR"
      ],
      "destinationCountry": "DEU",
      "active": true,
      "expectedBeneficiaryReceipt": "T+1",
      "chargeCodesRules": [
        {
          "type": "PRIORITY",
          "currencies": [
            "EUR"
          ],
          "chargeCodes": [
            "OUR"
          ]
        }
      ],
      "countryPurposeCodeRequired": false,
      "referenceRule": {
        "fieldName": "paymentReference",
        "required": true,
        "regex": "^[A-Za-z0-9\\s.,-]{1,35}$",
        "message": "Payment reference is invalid"
      },
      "recipientValidationCriteria": {
        "recipientAccountTypes": [
          "CACC"
        ],
        "residentTypes": [
          "NON_RESIDENT"
        ],
        "recipientTypes": [
          "COMPANY"
        ],
        "addressRules": [
          {
            "fieldName": "postalCode",
            "required": true,
            "regex": ""
          }
        ],
        "recipientBankRules": [
          {
            "fieldName": "branchName",
            "required": false,
            "regex": ""
          }
        ],
        "accountIdentifierGroups": [
          [
            {
              "fieldName": "IBAN",
              "required": true,
              "regex": ""
            },
            {
              "fieldName": "BIC",
              "required": false,
              "regex": ""
            }
          ],
          [
            {
              "fieldName": "ACCOUNT_NUMBER",
              "required": true,
              "regex": ""
            },
            {
              "fieldName": "ROUTING_NUMBER",
              "required": true,
              "regex": ""
            }
          ]
        ],
        "walletDetailsRules": [
          {
            "fieldName": "wallet_handle",
            "required": false,
            "regex": ""
          },
          {
            "fieldName": "wallet_provider",
            "required": false,
            "regex": ""
          }
        ],
        "individualRules": [
          {
            "fieldName": "lastName",
            "required": true,
            "regex": ""
          }
        ],
        "companyRules": [
          {
            "fieldName": "registrationNumber",
            "required": false,
            "regex": ""
          }
        ]
      },
      "provider": "PARTNER_BANK_A",
      "walletNames": [
        "string"
      ],
      "walletNetworks": [
        "string"
      ],
      "allowedPaymentPurposes": [
        "GOODS_AND_SERVICES"
      ]
    }
  ],
  "pagination": {
    "limit": 25,
    "page": 2,
    "totalItems": 237,
    "hasNext": true
  }
}
```

You can tell whether a field is required or not by checking the Boolean value against the `required` field. If we had this information, we had everything we need to explain and document each payment rail for each country.

I was able to set up 

## MarkDoc/Redocly research